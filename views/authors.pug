extends layout 

block content 
    h1= title 
    if authors.length 
        ul 
        each author in authors 
            li 
                a(href=author.url) #{author.name}
                |  (#{author.lifespan})
    else 
        p There are no authors.